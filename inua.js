// script.js

// Example form validation (for simplicity)
document.querySelectorAll("form").forEach(form => {
    form.addEventListener("submit", (event) => {
      event.preventDefault();
      alert("Form submitted successfully! Thank you for engaging with Inua Mwanaume Foundation.");
    });
  });
  